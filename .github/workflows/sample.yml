
name: shell-commands

on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: string
              run: echo "hello"
            - name: multiple script
              run:
                node -v
                npm -v
            

